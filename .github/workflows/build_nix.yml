name: "Build Nix package on self-hosted runner"

on:
  push:

jobs:
  build:
    runs-on: [self-hosted, nixos]
    steps:
      - uses: actions/checkout@v4
      # nix is already on the runner's PATH — no install-nix-action needed
      - name: Building package
        run: nix build
